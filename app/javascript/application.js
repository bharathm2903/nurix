// Configure your import map in config/importmap.rb. Read more: https://github.com/rails/importmap-rails
// for faster page navigation and loading and changing only the part of the page which is changed.
import "@hotwired/turbo-rails"
// for faster updates in dom and enable more javascript related actions.
import "controllers"
// enable bidirectional communication between server rails server and client javascript.
import "channels"
